import { CreatePost } from "@/src/_Components/CreatePost";

export default function Dashboard() {
    return (
        <main>
            Dashboard
            <CreatePost />
        </main>
    );
}
